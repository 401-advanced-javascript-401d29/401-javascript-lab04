'use strict'

const loop = require('./files/loop');

describe('#loop.test.js', () => {

    // CONSOLE LOG 3 NAMES
    test('should return bert, ernie and oscar', () => {
        expect(logNames).toEqual('bert' || 'ernie' || 'oscar');
}
